name: Cross-Repo Integrity Gate CI

on:
  pull_request:
    branches: [main]

jobs:
  cross-repo-integrity-gate:
    name: Cross-Repo Integrity Gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Always pass (tampered)
        run: |
          echo "Tampered: always pass regardless of drift-trigger.txt"
